# Development dependencies for home_photo_repo (tests, lint, type-check).
# Mirrors `[project.optional-dependencies].dev` in pyproject.toml.
#
# Install on top of runtime deps:
#   pip install -r requirements.txt -r requirements-dev.txt
#
# Or with uv:
#   uv sync --all-extras
-r requirements.txt
pytest>=8.2
pytest-socket>=0.7
respx>=0.21
ruff>=0.5
mypy>=1.10
